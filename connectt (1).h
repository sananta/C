/* This file lists the functions within the file connecntt.c, and is used to connect connecntt.c to connectn.c since the functions in both of these files are together used to run the whole program */
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int get_play(int rows, int columns);
void play_game(int rows, int columns, int point, char* playgrid[columns]);
